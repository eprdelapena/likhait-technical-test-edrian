/**
 * Common constants used across the application
 * Provides a central place to define values that are reused in multiple modules
 */
export enum ECommon {
    /**
     * Base API URL for all backend requests
     * Can be updated here to switch environments (development, staging, production)
     */
    BASE_API_URL = "http://localhost:3000"
}